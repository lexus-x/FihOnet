.semantic_scholar_mcp/
.DS_Store
Thumbs.db
node_modules/
.env
*.log

# Python cache
__pycache__/
*.pyc

# Raw data, checkpoints, and large binaries
data/
checkpoints/
outputs/
*.pt
*.pth

# Submission archives (large binaries; submissions/VERSIONS.md is force-tracked as the index)
submissions/
*.zip

# Local backup mirror (full snapshot of the GPU box, not tracked)
onet_backup/

# Competition starter kits + derived data artifacts (e.g. class_image_counts.csv)
competition/
