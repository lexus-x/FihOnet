.semantic_scholar_mcp/
.DS_Store
Thumbs.db
node_modules/
.env
*.log

# Python cache
__pycache__/
*.pyc

# Raw data, checkpoints, and large binaries
data/
checkpoints/
outputs/
*.pt
*.pth

# Submission archives (large binary)
submissions/
*.zip

# Local backup mirror (full remote snapshot, not tracked)
onet_backup/

# Large data CSV (623KB class distribution — tracked separately via docs)
class_image_counts.csv

# Competition starter kits
files from web of codeforece/
